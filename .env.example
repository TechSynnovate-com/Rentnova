# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here

# Instructions to get Firebase credentials:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new Firebase project or select existing one
# 3. Click "Add app" and select Web platform (</>)
# 4. Register your app with a nickname
# 5. In the Firebase SDK configuration, you'll find:
#    - apiKey (VITE_FIREBASE_API_KEY)
#    - projectId (VITE_FIREBASE_PROJECT_ID) 
#    - appId (VITE_FIREBASE_APP_ID)
#    - messagingSenderId (VITE_FIREBASE_MESSAGING_SENDER_ID)
# 6. Enable Authentication in Firebase Console:
#    - Go to Authentication > Sign-in method
#    - Enable Email/Password and Google sign-in
#    - Add your domain to Authorized domains
# 7. Set up Firestore Database:
#    - Go to Firestore Database
#    - Create database in production mode
# 8. Set up Storage:
#    - Go to Storage
#    - Get started with default rules

# Copy this file to .env and replace the placeholder values with your actual Firebase credentials
